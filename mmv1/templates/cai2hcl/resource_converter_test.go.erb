<%=lines(autogen_notice(:go, pwd))-%>

package <%= object.__product.name.downcase -%>

import (
    "fmt"
	"testing"

	cai2hcl_testing "github.com/GoogleCloudPlatform/terraform-google-conversion/v5/cai2hcl/testing"
)

<%
    resource_name = object.__product.name.downcase + object.name
    converter_file_name = "#{object.__product.name.downcase}_#{object.name.underscore}"
%>

func Test<%=resource_name-%>(t *testing.T) {
	cai2hcl_testing.AssertTestFiles(
		t,
		UberConverter,
		"./testdata",
		[]string { "<%=converter_file_name-%>" })
}
